- content_for :title, 'New User'
- content_for :action_bar, link_to('Cancel', users_path, :class => 'button btn btn-warning')

= form_for @user do |f|

  = render 'fields', :f => f

  %div.buttons= f.submit 'Add User', :class => 'btn btn-primary'