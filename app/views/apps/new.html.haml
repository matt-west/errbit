- content_for :title, 'Add App'
- content_for :action_bar do
  = link_to_copy_attributes_from_other_app
  = link_to('Cancel', apps_path, :class => 'button btn btn-warning')

= form_for @app do |f|

  = render 'fields', :f => f

  %div.buttons= f.submit 'Add App', :class => 'btn btn-primary'

